# 技能仓库实施计划

- [x] 1. 建立技能仓库数据模型和持久化
  - 在 `src/models.rs` 定义本地技能、来源配置、搜索结果、导入预览与审计记录。
  - 在 `src/state.rs` 增加独立状态文件的加载、保存和操作审计。
  - [x] 1.1 编写状态层单元测试
  - 验证配置恢复、预览过期与审计记录。

- [ ] 2. 实现本地技能包服务
  - 新建 `src/skill_repository.rs`，实现目录扫描、`SKILL.md` 解析、上传预览、校验、原子导入、替换和删除确认。
  - 实施文件数量、单文件体积、总容量、路径边界和符号链接校验。
  - [ ] 2.1 编写技能包服务单元测试
  - 验证有效包、缺失 `SKILL.md`、重复根目录、路径穿越、冲突和替换恢复。

- [ ] 3. 实现公开来源搜索服务
  - 新建 `src/skill_sources.rs`，定义统一来源适配器接口。
  - 实现 GitHub、SkillHub 和自定义公开索引适配器。
  - 实现来源并发搜索、超时、结果标准化和失败隔离。
  - [ ] 3.1 编写来源适配器单元测试
  - 验证搜索转换、`SKILL.md` 定位、来源异常和结果去重。

- [ ] 4. 提供技能仓库管理 API
  - 在 `src/admin.rs` 实现本地列表、详情、上传预览、导入、替换、删除、搜索和来源配置接口。
  - 在 `src/main.rs` 注册路由并沿用管理员会话鉴权。
  - [ ] 4.1 编写管理接口测试
  - 验证鉴权、预览确认、冲突、删除确认与来源配置校验。

- [ ] 5. 实现技能仓库一级功能页面
  - 在 `static/index.html` 添加“技能仓库”导航与本地技能、联网搜索、来源设置视图。
  - 在 `static/app.js` 实现搜索、预览、确认导入、冲突处理和本地技能详情交互。
  - 在 `static/style.css` 增加结果卡片、来源状态和响应式布局。
  - [ ] 5.1 执行前端交互验证
  - 验证导航切换、来源失败展示、导入确认、冲突和删除二次确认。

- [ ] 6. 完成全量验证
  - 运行 Rust 全量测试、静态脚本检查、格式检查和构建检查。
