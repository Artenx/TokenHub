# 需求实施计划

- [x] 1. 建立模型评测数据模型和独立持久化
  - 在 `src/models.rs` 定义任务、样本、尝试、评审结果、端点摘要和请求响应模型，覆盖需求 1.1、2.1、3.5、5.1。
  - 在 `src/state.rs` 增加独立评测状态文件的加载、保存、创建、取消和摘要计算，覆盖需求 1.4、2.2、5.2、5.3。
  - [x] 1.1 编写状态层单元测试
    - 验证三次尝试、成功率、中位数、持久化恢复、取消和输出截断，覆盖设计正确性属性。

- [x] 2. 实现串行评测执行器和自动评审
  - 新建评测服务，以指定端点执行固定模型和样本，记录 TTFT、总耗时、Token、输出和异常，覆盖需求 1.2、1.5、1.6、2.1、3.1。
  - 实现任务内评审端点调用、百分制 JSON 解析和失败隔离，覆盖需求 3.2、3.3、3.6。
  - [x] 2.1 编写执行器与评审器单元测试
    - 验证端点隔离、流式内容聚合、评分字段校验和评审失败保留性能结果。

- [x] 3. 提供模型评测管理接口
  - 实现任务创建、列表、详情、取消和候选端点模型支持状态接口，覆盖需求 1.1、1.3、2.3、4.4。
  - 注册管理路由并沿用现有管理员鉴权和响应约定。
  - [x] 3.1 编写管理接口测试
    - 验证鉴权、请求校验、任务查询、取消和候选端点筛选。

- [x] 4. 实现模型评测一级功能页面
  - 在管理后台增加“模型评测”入口、评测任务和运行监控页签，覆盖需求 4.1、4.2、4.3。
  - 实现任务创建器、任务列表、端点性能排序、样本并排输出与评审分数展示，覆盖需求 1.1、2.3、3.1、4.4。
  - [x] 4.1 编写前端交互验证
    - 验证表单校验、任务状态刷新、结果排序、输出截断和失败状态展示。

- [x] 5. 确保所有测试通过,如有疑问请询问用户
  - 运行 Rust 全量测试、静态脚本检查和构建检查。
